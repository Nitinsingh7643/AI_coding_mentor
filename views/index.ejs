<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>AI Coding Mentor</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(145deg, #0a0e17, #1a2332);
      color: #e0e6ed;
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      transition: all 0.5s ease;
    }
    .light-mode {
      background: linear-gradient(145deg, #f5f7fa, #c3cfe2);
      color: #1a2332;
    }
    .container {
      max-width: 900px;
      padding: 20px;
    }
    .card-custom {
      background: rgba(20, 25, 38, 0.9);
      border: none;
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }
    .card-custom:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.6);
    }
    .light-mode .card-custom {
      background: rgba(255, 255, 255, 0.95);
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
    }
    .btn-modern {
      background: linear-gradient(135deg, #00ddeb, #007bff);
      border: none;
      color: #fff;
      font-size: 16px;
      font-weight: 600;
      border-radius: 12px;
      padding: 14px 24px;
      width: 100%;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.4s ease;
    }
    .btn-modern:hover {
      background: linear-gradient(135deg, #007bff, #00ddeb);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0, 125, 255, 0.4);
    }
    .textarea-custom {
      background: rgba(10, 14, 23, 0.8);
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: #e0e6ed;
      border-radius: 12px;
      padding: 16px;
      resize: vertical;
      font-size: 15px;
      transition: all 0.3s ease;
    }
    .textarea-custom::placeholder {
      color: #a0a9b8;
    }
    .textarea-custom:focus {
      border-color: #00ddeb;
      background: rgba(20, 25, 38, 0.95);
      box-shadow: 0 0 10px rgba(0, 221, 235, 0.3);
      outline: none;
    }
    .light-mode .textarea-custom {
      background: #fff;
      border: 1px solid #d0d7de;
      color: #1a2332;
    }
    .light-mode .textarea-custom::placeholder {
      color: #6b7280;
    }
    .hero-section {
      text-align: center;
      padding: 80px 20px 60px;
      background: linear-gradient(145deg, #141926, #0a0e17);
      border-radius: 20px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
      margin-bottom: 40px;
      position: relative;
      animation: fadeIn 1.2s ease-in-out;
    }
    .light-mode .hero-section {
      background: linear-gradient(145deg, #ffffff, #f5f7fa);
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.05);
    }
    .toggle-dark-mode {
      cursor: pointer;
      font-size: 14px;
      font-weight: 600;
      background: none;
      border: none;
      color: #00ddeb;
      position: absolute;
      top: 20px;
      right: 20px;
      padding: 8px 16px;
      border-radius: 20px;
      transition: all 0.3s ease;
    }
    .toggle-dark-mode:hover {
      background: rgba(0, 221, 235, 0.1);
      transform: scale(1.05);
    }
    .light-mode .toggle-dark-mode {
      color: #007bff;
    }
    .hero-title {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 20px;
      font-size: 32px;
      font-weight: 700;
      animation: fadeSlideIn 1s ease-in-out;
    }
    .hero-title h1 {
      background: linear-gradient(135deg, #00ddeb, #007bff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.2);
      font-size: 42px;
      font-weight: 800;
      letter-spacing: 2px;
      transition: transform 0.3s ease;
    }
    .hero-title h1:hover {
      transform: scale(1.03);
    }
    .light-mode .hero-title h1 {
      background: linear-gradient(135deg, #1a73e8, #003087); /* Darker, more contrasting gradient for light mode */
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.1); /* Softer shadow for light mode */
    }
    .gemini-logo {
      width: 70px;
      height: auto;
      filter: drop-shadow(0 4px 10px rgba(0, 0, 0, 0.3));
      animation: float 3s ease-in-out infinite;
    }
    .suggestions-output pre {
      background: rgba(10, 14, 23, 0.9);
      padding: 20px;
      border-radius: 12px;
      font-size: 14px;
      color: #e0e6ed;
      white-space: pre-wrap;
      box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.3);
    }
    .light-mode .suggestions-output pre {
      background: #f8fafc;
      color: #1a2332;
      box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.05);
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    @keyframes fadeSlideIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="hero-section">
      <button class="toggle-dark-mode" onclick="toggleDarkMode()">🌙 Dark Mode</button>
      <div class="hero-title">
        <img src="download (1).png" alt="Gemini AI Logo" class="gemini-logo" />
        <h1 class="m-0">AI CODING MENTOR</h1>
      </div>
      <p class="mt-3">Elevate your code with intelligent, actionable insights Powered By <b>Gemini AI</b>.</p>
    </div>

    <div class="card-custom">
      <form method="POST" action="/">
        <div class="mb-4">
          <label for="code" class="form-label fw-semibold">Your Code</label>
          <textarea name="code" id="code" class="form-control textarea-custom" rows="10" placeholder="Paste your code here..."><%= code %></textarea>
        </div>
        <button type="submit" class="btn-modern">✨ Get AI Insights</button>
      </form>
    </div>

    <% if (suggestions) { %>
      <div class="mt-5 card-custom suggestions-output">
        <h4 class="mb-3 fw-semibold">🔍 AI Insights</h4>
        <pre><%= suggestions %></pre>
      </div>
    <% } %>
  </div>

  <script>
    function toggleDarkMode() {
      document.body.classList.toggle('light-mode');
      const button = document.querySelector('.toggle-dark-mode');
      button.innerHTML = document.body.classList.contains('light-mode') ? '🌑 Light Mode' : '🌙 Dark Mode';
    }
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>